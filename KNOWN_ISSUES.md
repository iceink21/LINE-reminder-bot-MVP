# Known Issues

ปัญหาที่ **รู้แล้ว ยอมรับแล้ว และยังไม่แก้** — บันทึกไว้เพื่อไม่ให้หายไปกับรอบรีวิว
ถ้าแก้ข้อไหนเสร็จ ให้ลบออกจากไฟล์นี้พร้อมกับ commit ที่แก้

---

## H-1 — `"วันอาทิตย์สัปดาห์หน้า"` ได้วันพุธ

**สถานะ:** เปิด · ความรุนแรง: สูง · พบเมื่อ 2026-09-16 (QC gate ของ `776a3ae`)

**Repro** (วันนี้ = พุธ 2026-09-16 ICT):

```
"ส่งงานวันอาทิตย์สัปดาห์หน้า"   →  2026-09-23 (พุธ)
```

ถูกต้องควรเป็น 2026-09-20 หรือ 2026-09-27 (อาทิตย์) — **ผิดทั้งสองการตีความ** เพราะ 09-23 ไม่ใช่วันอาทิตย์เลย

**Root cause** — `src/datetime.js`, `BARE_WEEK_REFERENCE_RE`:

```js
/(?:(?<!วัน\s?)อาทิตย์|สัปดาห์)\s?(?:หน้า|ที่จะถึง)/
```

negative lookbehind `(?<!วัน\s?)` ผูกกับ **สาขา `อาทิตย์` เท่านั้น** สาขา `สัปดาห์` ไม่มีการ์ดเลย (ตั้งใจ เพราะ `สัปดาห์` เดี่ยว ๆ แปลว่า "สัปดาห์" แน่นอน) พอผู้ใช้พูดว่า `วันอาทิตย์สัปดาห์หน้า` — ซึ่งเป็นภาษาไทยปกติ แปลว่า "วันอาทิตย์ของสัปดาห์หน้า" — คำว่า `สัปดาห์หน้า` ที่อยู่ท้ายประโยคไปเข้าสาขาที่ไม่มีการ์ด แล้ว `+7 วัน` ก็ทับ `relative_weekday = "อาทิตย์"` ทิ้ง

**ทิศทางที่ควรแก้:** เมื่อในข้อความมีชื่อวันที่มี `วัน` นำหน้าอยู่แล้ว คำบอกสัปดาห์ควร **เลื่อน** การคำนวณวันนั้น ไม่ใช่ **แทนที่** มันทั้งหมด

---

## H-2 — คำว่า "สัปดาห์หน้า" ในคนละประโยคย่อย ทับกำหนดส่งที่ผู้ใช้ระบุชัด

**สถานะ:** เปิด · ความรุนแรง: สูง · พบเมื่อ 2026-09-16 (QC gate ของ `776a3ae`)

**Repro** (วันนี้ = พุธ 2026-09-16 ICT) — ยืนยันสดแล้ว โมเดลตอบ `relative_weekday = "ศุกร์"` **ถูกต้อง** แต่โค้ดทิ้งคำตอบนั้น:

```
"อาทิตย์หน้ามีประชุม ส่งงานวันศุกร์"      →  2026-09-23T02:00:00.000Z   ผิด (ควรเป็นศุกร์ 09-18)
"ส่งงานวันศุกร์"                          →  2026-09-18T02:00:00.000Z   ถูก
"สัปดาห์หน้ามีสอบ ส่งรายงาน 25 กันยายน"   →  2026-09-23                 ผิด (ควรเป็น 25 ก.ย.)
```

**Root cause** — `src/gemini.js`, `finalizeResult()`:

```js
const bareWeekDate = recurring ? null : resolveBareWeekReference(now, userText);
```

กฎข้อ 2 ยิง **โดยไม่มีเงื่อนไข** ขอแค่เจอคำบอกสัปดาห์ "ที่ไหนก็ได้" ในข้อความ แล้วมันอยู่เหนือทั้ง `relative_weekday` และ `deadline_iso` ตามลำดับความสำคัญ ผลคือประโยคที่พูดถึงสัปดาห์หน้าแบบ**ผ่าน ๆ** (บริบท: มีประชุม/มีสอบ) ไปทับกำหนดส่งจริงที่ผู้ใช้ระบุไว้ในอีกประโยคย่อยหนึ่ง

regex เป็น **substring match ไม่ใช่การ parse ประโยค** จึงแยกไม่ออกว่าคำบอกสัปดาห์นั้นขยาย "งาน" หรือขยาย "ประชุม"

**ทิศทางที่ควรแก้:** ยิงกฎข้อ 2 เฉพาะตอนที่โมเดล **ไม่ได้** ให้ชื่อวันที่มี `วัน` นำหน้า หรือวันที่ระบุตรง ๆ มาด้วย — หรือบังคับให้คำบอกสัปดาห์ต้องอยู่ติดกับคำกริยาของงาน

---

## M-1 — `REQUEST_TIMEOUT_MS` ก้อนเดียวคุมทั้งสองขา แต่ Gemini fallback ไม่ทัน

**สถานะ:** ✅ **แก้แล้ว 2026-09-21** (แยกเป็นสองงบใน `src/config.js` + `src/gemini.js`)

เดิม `src/gemini.js` ใช้ `REQUEST_TIMEOUT_MS = 12000` ค่าเดียวกับทั้ง `callOpenRouter()` และ `callGemini()`
เลข 12s ตั้งไว้ราวกับว่าตัวบีบคือ **webhook** ซึ่ง **ไม่จริง** — `src/index.js` ตอบ `res.status(200).end()`
**ก่อน** เรียก `handleEvent(event)` และจงใจไม่ `await` อยู่แล้ว การ ack กับการประมวลผลจึงแยกกันตั้งแต่แรก
ตัวบีบจริงคือ **อายุของ LINE reply token (~60 วินาที ใช้ได้ครั้งเดียว)** ต่างหาก
ผลคือขา fallback (gemini-3.6-flash วัดได้ 11.5–22.6s) ถูกตัดหัวที่ 12s ทั้งที่ยังมีเวลาเหลืออีกเกือบ 50 วินาที

**ทางแก้ที่ลงจริง** — สองงบแยกกัน ปรับผ่าน env ได้ทั้งคู่:

| งบ | ค่าเริ่มต้น | env |
|---|---|---|
| ขาหลัก (`callOpenRouter`) | 25,000 ms | `LLM_PRIMARY_TIMEOUT_MS` |
| ขา fallback (`callGemini`) | 20,000 ms | `LLM_FALLBACK_TIMEOUT_MS` |

**ข้อผูกมัดที่ห้ามลืม (แก้ใหม่ 2026-09-21 — ของเดิมในหัวข้อนี้ผิด):** สมการ
`PRIMARY + FALLBACK + เวลายิง reply < 60s` คิดครอบแค่ **หนึ่งเชน** แต่ **หนึ่งอีเวนต์รันได้สองเชน**
(`parseReminder` แล้วต่อด้วย `chatReply` บน reply token ใบเดิม) 45s สองรอบ = 90s ชน 60s เข้าเต็ม ๆ
ดู **M-1b** ด้านล่าง — ข้อผูกมัดที่ถูกต้องคือ `LLM_EVENT_BUDGET_MS + เวลายิง reply < 60s`
ซึ่งเป็นสมการที่ `assertConfig()` ใน `config.js` ตรวจตอนสตาร์ตแล้ว `console.warn` ถ้าเกิน

**ยังคงส่งด้วย `reply` ไม่ใช่ `push`:** reply ฟรีและไม่จำกัด ส่วน push จำกัด 200 ข้อความ/เดือน (~6/วัน)
การย้ายไป push เพื่อซื้อเวลาจึงแย่กว่าเดิม ในเมื่อหน้าต่าง 60 วินาทีของ reply ยังใช้ไม่หมดด้วยซ้ำ

**อาการตกค้างที่ยอมรับแล้ว:** งบที่กว้างขึ้นทำให้เส้นทางช้าสุดมีโอกาส (น้อย) เลย 60s จนโทเคนตาย
กรณีนั้น `src/line.js` จะ log บรรทัด `REPLY TOKEN DEAD` แยกชัดเจน — จงใจไม่ fallback ไป push อัตโนมัติ
เพราะโควตา push เป็นของผู้ใช้ ต้องให้ผู้ใช้ตัดสินใจเอง

---

## M-1b — งบ 25s/20s คิดครอบแค่หนึ่งเชน แต่ทางข้อความอิสระรันสองเชน

**สถานะ:** ✅ **แก้แล้ว 2026-09-21** (deadline ต่ออีเวนต์ + ตัดเชนที่สองเมื่อปลายทางล่ม)

การแยกงบใน M-1 แก้ปัญหาของตัวเองได้จริง แต่สร้าง **regression** ขึ้นมาพร้อมกัน เพราะคำนวณจากสมมติฐานว่า
หนึ่งอีเวนต์เรียก LLM หนึ่งเชน ซึ่งไม่จริงสำหรับทางข้อความอิสระ:

```
parseReminder : 25s primary + 20s fallback = 45s
chatReply     : 25s primary + 20s fallback = 45s   <- เชนที่สอง บน reply token ใบเดิม
                                              ----
                                               90s  >  60s
```

`catch` ใน `handleFreeText()` กว้างเกินไป: `'network'` (ซึ่งคือหน้าตาของ AbortSignal timeout), `'http_402'`,
`'http_503'` ตกมาที่ `replyAsChat()` เหมือนกันหมด ไม่ใช่แค่ `'low_confidence'` ส่วน `assertConfig()`
คำนวณ `25000 + 20000 + 10000 = 55000 < 60000` แล้วเงียบ เลขคณิตถูก แต่**ไปเฝ้าคนละสมการ**

เพดาน 12s ก้อนเดียวของเดิมรอดมาได้เพราะบังเอิญ (12x4 = 48s) การขยายงบต่อเชนจึงทำให้ config
ที่ "ปลอดภัยโดยบังเอิญ" พังลง และไม่ใช่เคสสมมติ การวัดที่เป็นจุดตั้งต้นของเรื่องนี้ทั้งหมด
คือคอลฝั่ง free tier ที่ **ค้าง 120 วินาที แล้วคืน HTTP 200 ตัวเปล่า** error เร็ว ๆ (402/503) ยุบทั้งสองเชน
เหลือ ~1s ไม่เป็นไร แต่**อาการค้าง**คือสิ่งที่งบนี้มีไว้กัน และเป็นสิ่งที่ทำให้งบพังพอดี

**ทางแก้ที่ลงจริง** สี่อย่าง:

| # | แก้อะไร | ที่ไหน |
|---|---|---|
| 1 | งบของขา fallback คลุม **ทั้งขา** (`legDeadline` คำนวณครั้งเดียวก่อนลูป retry) ไม่ใช่ต่อ attempt ของเดิม `AbortSignal.timeout()` อยู่ข้างในลูป ทำให้ขา "20s" วิ่งจริงได้ ~76s (3 fetch x 20s + 2 sleep x 8s) | `src/gemini.js` `callGemini()` |
| 2 | ปลายทางล่ม **ไม่เรียกเชนที่สอง** `isProviderFailure()` แยก "ปลายทางพัง" (`network`, `http_*`, `bad_json`, ...) ออกจาก "คำตัดสินต่อข้อความผู้ใช้" (`low_confidence`, `no_title`, `no_deadline`) เฉพาะอย่างหลังเท่านั้นที่ได้คุยแชต | `src/gemini.js`, `src/webhook.js` |
| 3 | **deadline เดียวต่ออีเวนต์** (`LLM_EVENT_BUDGET_MS` ค่าเริ่มต้น 45,000 ms) ตั้งตอนรับอีเวนต์แล้วส่งต่อทุก LLM call ของอีเวนต์นั้น แต่ละขาได้ `min(งบตัวเอง, เวลาที่เหลือ)` | `src/webhook.js` -> `runWithFallback()` |
| 4 | `assertConfig()` ย้ายไปตรวจ `eventBudgetMs + replyReserveMs < replyTokenTtlMs` แทนผลรวมของเชนเดียว | `src/config.js` |

**ผลข้างเคียงที่ตั้งใจ:** เมื่อปลายทางล่ม งานที่มีกำหนดส่งจะได้ ack แล้ว **ไม่มีการตั้งเตือน** ข้อความ ack เดิม
("ตอนนี้ผมตอบยาว ๆ ไม่ไหวแป๊บนึง") ไม่ได้บอกเรื่องนี้เลย จึงเขียนใหม่ให้บอกตรง ๆ ว่ายังไม่ได้ตั้งเตือน
และให้ผู้ใช้พิมพ์ส่งมาใหม่ (`src/messages.js` `inboxAckText()`)

**เก็บไว้เป็นบทเรียน:** invariant ที่เขียนไว้ถูกต้องหลายที่ ไม่ได้แปลว่ามันเฝ้า expression ที่ถูก
สมการนี้ถูกเขียนย้ำไว้ 5 จุด (`config.js`, `gemini.js`, `line.js`, `README.md`, ไฟล์นี้) และ **ผิดพร้อมกันทั้ง 5 จุด**

---

## M-2 — ขา fallback (`gemini-3.6-flash`) ล่มเองเป็นระยะ

**สถานะ:** เปิด · ความรุนแรง: กลาง · วัดเมื่อ 2026-09-21

ทดสอบสด 5 ครั้ง ได้ HTTP 503 `"model is overloaded"` **3 ใน 5** ครั้ง — เป็นปัญหาฝั่งผู้ให้บริการ แก้จากฝั่งเราไม่ได้
ผลคือ "ตกมาถึง fallback" ไม่ได้แปลว่า "ได้คำตอบแน่" อีกต่อไป งบ 20s ช่วยได้แค่เคสที่ Gemini ยอมตอบเท่านั้น
ตอนนี้บรรทัด log ตอน fallback บอกไว้แล้วว่าขานี้ flaky เพื่อให้อ่าน log ย้อนหลังแล้วแยกออกว่า
"ช้าเกินงบ" กับ "ปลายทางล่ม" คนละเรื่องกัน

---

## M-3 — ขา fallback มีเพดานแข็ง 20 ครั้ง/วัน ไม่ใช่ตาข่ายรองรับจริง

**สถานะ:** เปิด · ความรุนแรง: กลาง · วัดเมื่อ 2026-09-21

`gemini-3.6-flash` บน free tier ติดโควตา `GenerateRequestsPerDayPerProjectPerModel`
**เพดานแข็ง 20 request/วัน** — ยืนยันด้วยการ probe ตรงแล้วได้ `RESOURCE_EXHAUSTED`
โควตานี้หมดได้ภายใน smoke session เดียว (รอบทดสอบ 2026-09-21 ใช้หมดเอง)

ผลต่อการออกแบบ: fallback **ไม่ใช่ชั้น redundancy** แต่เป็นบัฟเฟอร์บาง ๆ
ถ้า `inception/mercury-2.5` ล่ม ระบบไม่ได้ degrade ไปหา Gemini แต่ degrade ไปที่ inbox ack
ภายในราวหนึ่งชั่วโมงของการใช้งานปกติ — คนละเรื่องกับที่เคยเข้าใจว่า "primary + fallback"

ซ้ำร้าย เส้นทาง `no_title`/`no_deadline` เรียก Gemini ได้ **2 ครั้งต่อ 1 event**
(ดู N-2 ในรายงาน QC) ซึ่งหั่นเพดานจริงเหลือ ~10 event/วัน

**สิ่งที่กันความเสียหายไว้แล้ว:** `inboxAckText()` บอกตรง ๆ ว่า "ยังไม่ได้ตั้งเตือน"
ดังนั้นโควตาหมด = ผู้ใช้รู้ตัว ไม่ใช่งานหายเงียบ

---

## หมายเหตุประกอบ (ความรุนแรงต่ำ)

| # | อาการ | สถานะ |
|---|---|---|
| N-1 | `"เตรียมสไลด์ประชุมสัปดาห์หน้า"` → `+7d` | **ยอมรับ** — เตือนงานของประชุมสัปดาห์หน้าให้ตกสัปดาห์หน้า สมเหตุสมผลในโดเมนนี้ ผู้ใช้เห็นวันที่และ `/edit` ได้ |
| N-2 | `"อีกสองอาทิตย์หน้า"` (สองสัปดาห์) → `+7d` เร็วไปหนึ่งสัปดาห์ | เปิด |
| N-3 | `"สัปดาห์ถัดไป"` ไม่เข้า regex (ไม่มี `ถัดไป` ใน alternation) → ตกไปทางเดิม | เปิด |
| N-4 | เว้นวรรคสองครั้งหลัง `วัน` (`"วัน  อาทิตย์หน้า"`) ทำให้ lookbehind หลุด | เปิด, ผิวเผิน |

---

## O-1 — `ตี n` และ `n โมงเช้า` ไม่มีกฎใน `SYSTEM_RULES` เลย

**สถานะ:** เปิด · ความรุนแรง: กลาง · บันทึก 2026-09-21

`SYSTEM_RULES` มีตารางเทียบเวลาให้เฉพาะ `ทุ่ม` (`n ทุ่ม = (18+n):00`) กับ `คาบ N` เท่านั้น
ส่วน **`ตี n`** (ตี 1 = 01:00 ... ตี 5 = 05:00) และ **`n โมงเช้า`** (6 โมงเช้า = 06:00 ... 11 โมงเช้า = 11:00)
**ไม่มีกฎรองรับเลย** ค่าที่ได้จึงมาจาก prior ของโมเดลล้วน ๆ ซึ่งเป็นความเสี่ยง **คลาสเดียวกับบั๊ก `ทุ่ม`**
ที่เพิ่งแก้ไป (นาฬิกาหกชั่วโมงแบบไทย โมเดลคำนวณพลาดเป็นประจำ) แค่ยังไม่มีใครเจอกับตาเท่านั้น

`บ่าย` ก็ไม่ปลอดภัยเต็มที่: มีแค่ตัวอย่างเดี่ยว ๆ บรรทัดเดียว (`'- "บ่าย 3 โมง" = 15:00'`)
**ไม่มีสูตร** ฉะนั้น "บ่ายโมง" หรือ "บ่าย 4" ก็ยังต้องอาศัยการเดาของโมเดลอยู่ดี

**ทิศทางที่ควรแก้:** เติมตารางเทียบแบบเดียวกับ `ทุ่ม` ทั้งสามคำ (ตาราง ไม่ใช่สูตร โมเดลใช้ตารางได้ดีกว่า)
แล้วเขียน prompt-pin ใน `test/thai-parsing-rules.test.js` ตามแบบเดิม
**หมายเหตุ:** การพิสูจน์ว่าโมเดล *ตอบ* ถูกจริงต้องยิงสดเท่านั้น unit test พินได้แค่ว่าข้อความกฎยังอยู่

---

## O-2 — `isDeadReplyToken()` ผูกกับการที่ SDK ส่ง `body` มาเป็น string

**สถานะ:** เปิด · ความรุนแรง: ต่ำ (สังเกตการณ์) · บันทึก 2026-09-21

`src/line.js`:

```js
return /reply token/i.test(String(err.body || err.message || ''));
```

การแยก "โทเคนตาย" ออกจาก HTTP 400 อื่น ๆ อาศัยการที่ `@line/bot-sdk` ใส่ **ข้อความ** ของ error ไว้ใน
`err.body` ถ้า SDK เวอร์ชันหน้าเปลี่ยนไปใส่เป็น object (`{ message: ... }`) `String()` จะได้
`"[object Object]"` regex ไม่เจอ แล้ว **ฟังก์ชันคืน `false` เงียบ ๆ** ไม่ throw ไม่ log
อาการที่ตามมาคือบรรทัด `REPLY TOKEN DEAD` หายไปจาก log ทั้งที่ผู้ใช้ไม่ได้รับข้อความ
ซึ่งเป็นสัญญาณสำคัญที่สุดของ regression งบเวลา (ดู M-1b)

**ตอนนี้ยังไม่มีเทสต์ครอบเลยสักข้อ** `package.json` ตรึง `^9.5.0` ไว้ก็จริง แต่ `^` ปล่อย minor ผ่านได้

**ทิศทางที่ควรแก้:** unit test เล็ก ๆ ที่ยิง `isDeadReplyToken()` ด้วย error ทั้งสองรูป (`body` เป็น string
และเป็น object) แล้วทำให้ฟังก์ชันอ่าน `err.body?.message` ด้วย ต้อง export ฟังก์ชันออกมาก่อน (ตอนนี้เป็น module-private)

---

## บทเรียนจากรอบนี้

H-1 และ H-2 **รอดจาก unit test 43 ข้อและการทดสอบสดกับโมเดลจริง 9 รอบ** เพราะทั้ง test และ probe ใช้ประโยคชั้นเดียวที่มีรูปร่างเหมือนบั๊กที่กำลังแก้อยู่ทั้งหมด ไม่มีอันไหนเป็นประโยคหลายอนุประโยคหรือมีคำบอกวัน/สัปดาห์ปนกันเลย

เวลาเพิ่มกฎที่อ่าน **ข้อความดิบของผู้ใช้** ให้เขียนเคสทดสอบที่คำสำคัญไป **โผล่ในบริบทที่ไม่ได้ตั้งใจ** ด้วยเสมอ ไม่ใช่แค่บริบทที่ตั้งใจ
